<%= form_for(@income_entry) do |f| %>
  
  <% if @income_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@income_entry.errors.count, "error") %> prohibited this income_entry from being saved:</h2>

      <ul>
      <% @income_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :income_id, "Source" %><br />
    <%= collection_select(:income_entry, :income_id, Income.all, :id, :name) %>
  </div>

  <div class="form-group">
    <%= f.label :amount %><br />
    <%= f.number_field :amount, step: "any", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :date %><br />
    <%= f.date_select :date, class: "form-control" %>
  </div>
  
  <div class="form-group">
    <%= f.label :notes %><br />
    <%= f.text_area :notes, class: "form-control", size: "3x3" %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-default" %>
  </div>
<% end %>
